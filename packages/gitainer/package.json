{
  "name": "gitainer",
  "module": "src/server/server.ts",
  "type": "module",
  "scripts": {
    "start": "bun run --watch src/server/server.ts",
    "migrate": "bun run src/migration/PortainerMigration.ts",
    "test:docker:build": "docker build --target test -f Dockerfile -t gitainer-test ../..",
    "test:docker": "docker run --privileged --rm -w /app gitainer-test"
  },
  "dependencies": {
    "@infisical/sdk": "^4.0.6",
    "@types/bun": "^1.3.14",
    "concurrently": "^8.2.2",
    "hono": "^4.5.5",
    "js-yaml": "^4.1.0",
    "node-git-server": "^1.0.0",
    "simple-git": "^3.25.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  }
}